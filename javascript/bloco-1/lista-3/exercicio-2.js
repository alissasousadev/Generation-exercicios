const leia = require("readline-sync");

//leia 10 números inteiros e mostre na tela 
// quantos números são pares e quantos número são ímpares.

let pares = 0;
let impar = 0;

for (let i = 1; i <= 10; i++) {
    let numero = leia.questionInt(`Digite o ${i} numero:` );
    if (i % 2 === 0 && )
}
